<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Example</h5>
          <p>
            <strong>ID</strong>: </p>
            <ul>
              {{#each userInfo}}
              <li>{{user_first_name}} | {{user_last_name}} | {{user_email}}</li>
              {{/each}}
            </ul>

            <ul>
              {{#each projectInfo}}
              <li>{{project_name}}</li>
              {{/each}}
            </ul>
          <p>

            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
