{{> sidebar/sidebar taskNav=true}}

<div class="pageContentContainer">

  {{> subnav this}}

  <div class="listContainer">
    <h2>My Tasks</h2>
    <ul>
      {{#each tasks}}
        <li class="classLink">{{task_name}}</li>
      {{/each}}
    </ul>
  </div>
</div>