

<div class="subnav">
  {{#if this.isAdmin}}
    <a class="createProject"><i class="fas fa-plus-circle"></i>Create project</a>
  {{/if}}
  <a class="notifications"><i class="fas fa-bell"></i>Notifications</a>
</div>