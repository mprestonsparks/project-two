{{#if loggedIn}}

  {{> sidebar/sidebar isAdmin=isAdmin user=user}}

  <section>
    <h2>Team Members</h2>
    <ul>
      {{#each members}}
        <li>
          {{firstName}} {{lastName}}
        </li>
      {{/each}}
    </ul>
  </section>

{{else}}

  Log in stuff...

{{/if}}
